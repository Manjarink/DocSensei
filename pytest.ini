[pytest]
# Silence the langchain-community sunset DeprecationWarning.
# It is a package-level notice, not a code defect; our loaders still work correctly.
filterwarnings =
    ignore::DeprecationWarning:langchain_community
    ignore::DeprecationWarning:langchain_core
